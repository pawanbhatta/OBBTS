<%- include('partials/navbar.ejs') %>

    <div class="row mt-5">
        <div class="col-md-6 m-auto">
            <div class="card card-body">
                <h1 class="text-center mb-3">
                    <i class="fas fa-bus"></i> Add Seat
                </h1>
                <%- include('partials/messages.ejs') %>
                    <form action="/seat/create" method="POST">
                        <!-- <div class="form-group">
                            <label for="exampleSelect1">Select Bus</label>
                            <select class="form-control" id="bus" name="busId">
                        <% buses.forEach(function(bus){ %>
                          <option value="<%= bus._id %>"><%= bus.busName %></option>
                        <% }) %>
                      </select>
                        </div> -->
                        <div class="form-group">
                            <label for="seat">Seat</label>
                            <input type="seat" id="seat" name="seat" class="form-control" placeholder="Enter Seat" value="<%= typeof seat != 'undefined' ? seat : '' %>" required />
                        </div>
                        <button type="submit" class="btn btn-primary btn-block">
      Add Seat
    </button>
                    </form>
                    <p class="lead mt-4"><a href="/dashboard">Go Back</a></p>
            </div>
        </div>
    </div>